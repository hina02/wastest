[project]
name = "wastest-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic-ai[gemini]>=0.0.14",
    "lancedb>=0.20",
    "duckdb>=1.1",
    "streamlit>=1.40",
    "python-dotenv>=1.0",
    "google-genai>=1.0",
]

[tool.uv]
dev-dependencies = []
